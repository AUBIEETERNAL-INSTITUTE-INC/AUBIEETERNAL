# AUBIEETERNAL requirements.txt
# Updated for v8 — covers all modules

# ── Core UI ──────────────────────────────────────────────────────────────────
streamlit>=1.32.0

# ── AI / API ─────────────────────────────────────────────────────────────────
openai>=1.14.0
requests>=2.31.0

# ── Data + Viz ────────────────────────────────────────────────────────────────
pandas>=2.0.0
plotly>=5.18.0

# ── Bitcoin / Nostr ───────────────────────────────────────────────────────────
# (nostr uses requests only — no extra package needed)

# ── Living Lattice + Epistemic Commons ───────────────────────────────────────
# (uses stdlib only: json, hashlib, datetime, pathlib, socket)

# ── Sovereign Cashflow Game ───────────────────────────────────────────────────
# (uses stdlib only: json, random, datetime, pathlib)

# ── Offline runner ────────────────────────────────────────────────────────────
# (uses subprocess, os, sys — all stdlib)

# ── Utils ─────────────────────────────────────────────────────────────────────
python-dateutil>=2.8.2
pytz>=2023.3

# ── Community deployment (join-card QR codes) ────────────────────────────────
qrcode[pil]>=7.4

# ── QR Airlock (tools/qr_airlock — mounted on assistant_server.py) ───────────
# Offline decode of a scanned QR image -> payload string. Headless build: no
# GUI libs on the rig. numpy is already pulled in transitively but pinned here
# so a fresh install of just this module works. The module also needs the
# FastAPI stack (fastapi / pydantic / uvicorn) that assistant_server.py
# already requires.
opencv-python-headless>=4.9
numpy>=1.24
# pyzbar is the primary QR decoder (more robust on real photos than OpenCV's);
# decode.py falls back to cv2 automatically if it's missing. Needs the system
# library libzbar0:  sudo apt-get install libzbar0
pyzbar>=0.1.9
