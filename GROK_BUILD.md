# GROK_BUILD.md

Thin adapter for Grok Build CLI / worktree. This file does not age. Dated facts live only in `CURRENT.md`.

## First instruction

Read `CURRENT.md`, then change code.

You see the checkout. You do not see Grok chat memory. Chat-only artifacts (session `/home/workdir/artifacts`) are invisible until someone pushes them.

## Source of truth

GitHub `main` (`AUBIEETERNAL-INSTITUTE-INC/AUBIEETERNAL`). Pull / reset to `origin/main` before durable work. Do not invent files that `CURRENT.md` says are still conversation-only.

## After any durable decision

Update `CURRENT.md` in the **same commit**. Adapters (`GROK.md`, this file, `CLAUDE.md`) do not get a second copy of dates or hardware lists.

## Local Build path

`aubieeternal_build_code.py` + `aubieeternal_build/` + Sandbox Lab tab. That is the in-repo Build surface. Do not rewrite `app.py`, swarm, or `grok-principles.md` unless the task is those files.

## Operating axioms

`grok-principles.md`. Do not refresh that file as a side effect of a briefing pass.
