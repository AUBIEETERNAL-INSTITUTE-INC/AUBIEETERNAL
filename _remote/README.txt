_remote/  --  copies of code that authoritatively lives on an EDGE DEVICE, not the rig.

board/          current + historical copies of files from the UNO Q tutor board
                (100.66.110.65), pulled by tools/pull-board-files. These are the
                rig-side safety copy; the board is still where they run.
                *.2026-XX-XX-prototype = dated historical snapshots, not live.
sync.log        pull-board-files run log (gitignored)

Full edge-only file audit + the "devices are disposable" principle: see CLAUDE.md
section "Edge devices are disposable".
